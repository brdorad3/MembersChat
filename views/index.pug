extends layout

block content
  if user 
    h1 Welcome #{user.first_name} #{user.last_name} 
    a(href="/log-out") log out
  h1 Robnite
  p Chat with the members!
  if user
    if user.member
      p Create new